# What do the following method calls do (assume we reset numbers
# to the original array between method calls)?

numbers = [1,2,3,4,5]

## numbers.delete_at(1)
# This will remove the value specified at index 1, which is two (2)

## numbers.delete(1)
# This will remove the number 1 from the array